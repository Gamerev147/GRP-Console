///@description Animate and Function

if (animate = true) {
	
	if (window_width < max_window_width) {
		window_width += 40;	
	}
	
	if (window_height < max_window_height) {
		window_height += 40;	
	}
	
}

if (window_width = max_window_width) {
	animate = false;	
	animate_done = true;
}
